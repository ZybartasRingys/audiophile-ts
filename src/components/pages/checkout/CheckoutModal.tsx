import {
  Modal,
  ModalOverlay,
  ModalContent,
  ModalHeader,
  ModalFooter,
  ModalBody,
  ModalCloseButton,
  Button,
  useDisclosure,
} from '@chakra-ui/react'

const CheckoutModal = () => {
  const { isOpen, onOpen, onClose } = useDisclosure()
  return (
   
  )
}

export default CheckoutModal
